let favouriteDrink = "tea"; // creates a variable called "favorate drink" and stores the text "tea" in it
let age = 51;   // creates a variable called "age" and stores the value "51" in it
let name = "Steve"; // creates the variable "name" and stores the value "Steve" in it
console.log(`Hi, my name ${name}, I am ${age} and my favourite drink is ${favouriteDrink}`); 
// sends the text "Hi....etc" while inserting the variables by putting them within ${} 