let num = 16 // creates a variable and stores a value
if (num % 3 == 0 || num % 5 == 0) {  // if "num" is divisable 3 (if it is the remainder will be 0) or by 5 then output {} to comsole
    console.log(`The numer ${num} is divisable by 3 or 5`)
} else { console.log(`The number ${num} is not divisable by 3 or 5`)} // if not do this
